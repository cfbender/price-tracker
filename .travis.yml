language: node_js
services:
  - docker
branches:
  only:
    - master
env:
  - NODE_ENV=test
services:
  - mysql
cache:
  directories:
    - node_modules
before_install:
  - yarn
script:
  - docker-compose up
after_script:
  - docker-compose kill
